language: python
python:
  - "3.6"
services:
  - postgresql
# install dependencies
install:
  - pip install -r requirements.txt
before_script:
  -psql -U postgres -a -f database_setup.sql
script:
  - pytest